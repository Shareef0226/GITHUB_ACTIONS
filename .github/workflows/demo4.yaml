name: ENV_VAR_DEMO

on: push
# work flow level varibales
env:
  APP_ENV: "dev"
  
jobs:
  env_var_demo:
# job level varibales declartion  
    env:
      APP_ENV: "test"
      
    runs-on: ubuntu-latest
    steps:
      - name: PRINT THE ENV NAME
# step level varaibles declaration      
        env:
          APP_ENV: "prod"
          
        run: echo "env names is $APP_ENV"                   # ✅ Print the env name

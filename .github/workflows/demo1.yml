name: CI
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: hello_world
        run: echo Hello, world!
        
      - name: multiple shell commnads execution
        run: |
          echo: "ls"
          echo: "pwd"
        
